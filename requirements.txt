# culprit runtime dependencies.
#
# Deliberately short. Everything else this tool needs (sqlite3, xml.etree,
# grp/pwd, subprocess access to systemctl/journalctl/loginctl/lsblk/ip) ships
# with CPython or the base system, and the frontend has no build step and no
# npm dependencies at all.

fastapi>=0.115
uvicorn[standard]>=0.30
psutil>=6.0

# Optional, lazily imported, degrades to an explicit unavailable state:
#   nvidia-ml-py  -- per-process GPU stats on NVIDIA (imported as pynvml).
#                    Install it only if you have an NVIDIA card:
#                    pip install nvidia-ml-py
